# .completion

# This script must be executed before all other scripts with additions for tab completion.
# See https://itnext.io/programmable-completion-for-bash-on-macos-f81a0103080b

# Include also tab completion scripts in /usr/local/etc/bash_completion.d
export BASH_COMPLETION_COMPAT_DIR="/usr/local/etc/bash_completion.d"

# Activate tab completions for basic programs and provide convenience functions for other completion scripts
[[ -r "/usr/local/etc/profile.d/bash_completion.sh" ]] && . "/usr/local/etc/profile.d/bash_completion.sh"
