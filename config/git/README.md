
# Git configuration

Put your personal settings in `~/.gitconfig.custom`. This file is included in
the main `.gitconfig` file.

For example:

```
[user]
    name = Jo Doe
    email = jo.doe@web.de
```
